/**
 * PaymentModal.tsx — TypeScript declaration stub.
 *
 * Metro resolves PaymentModal.native.tsx / PaymentModal.web.tsx at runtime.
 * This file exists so that TypeScript can resolve
 *   `import PaymentModal from '../components/payment/PaymentModal'`
 * without errors.
 */
export { default } from './PaymentModal.native';
